{
  "name": "bridge-tools",
  "version": "0.1.0",
  "private": true,
  "description": "One-off helpers used across the bridge kit (cosmos_announce, etc.). Run `npm install` once.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ethers": "^6.13.0",
    "yaml": "^2.6.0"
  }
}
